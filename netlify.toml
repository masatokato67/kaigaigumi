[build]
  command = "node -v && npm -v && npm run build 2>&1; EXIT_CODE=$?; echo \"EXIT_CODE=$EXIT_CODE\"; exit $EXIT_CODE"
  publish = "out"

[build.environment]
  NODE_VERSION = "22"
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
  NEXT_PLUGIN_FORCE_RUN = "false"

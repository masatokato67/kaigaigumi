[build]
  command = "echo 'Node:' && node -v && echo 'npm:' && npm -v && echo 'Starting build...' && next build && echo 'Build completed successfully'"
  publish = "out"

[build.environment]
  NODE_VERSION = "22"
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
  NEXT_PLUGIN_FORCE_RUN = "false"
